# torch and torch-directml are reused from the existing AMD GPU environment
# (already installed: torch 2.4.x + torch-directml 0.2.5). Listed for reference.
torch==2.4.1
torch-directml==0.2.5
chess>=1.11
zstandard>=0.22
numpy>=1.26
fastapi>=0.111
uvicorn>=0.30
pytest>=8.0
httpx>=0.27
